name: Publish Docker
on:
  push:
    tags:
      - v*
    branches: # Temporary to test the action
      - chore/change-container-build-and-push
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@master
    - name: Publish to GitHub Container Registry
      uses: docker/build-push-action@v6
      with:
        username: ${{ secrets.GITHUB_ACTOR }}
        password: ${{ secrets.GITHUB_TOKEN }}
        repository: ghcr.io/libremfg/packml-simulator
        tags: |
          libremfg/packml-simulator:${{ github.ref_name }}
          libremfg/packml-simulator:latest
    - name: Publish to DockerHub Registry
      uses: elgohr/Publish-Docker-Github-Action@v5
      with:
        name: spruiktec/packml-simulator
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}